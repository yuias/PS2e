[package]
name = "ps2-app"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Native front-end for ps2-core (headless CLI for now)"

[dependencies]
ps2-core = { path = "../ps2-core" }
ps2-debug = { path = "../ps2-debug" }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
