[package]
name = "ps2-debug"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "LLDB-first gdb-remote debug stub for the PS2e core (EE and IOP targets)"

[dependencies]
ps2-core = { path = "../ps2-core" }
tracing = "0.1"
